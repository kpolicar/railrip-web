## Build:
This project is a custom SPA. all the source files are contained in the /app/ folder. The output (SPA) is automatically outputed to /index.html.
Use `node ./build.js` to build the application 